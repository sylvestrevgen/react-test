export const getPosts = state => state.posts;

export const getSinglePost = state => state.singlePost;

export const isLoading = state => state.loading;

export const getError = state => state.error;
